void main() {
  // Only references logo — banner, photo, and data.json are "unused"
  const logo = 'assets/images/logo.png';
  const homeIcon = 'assets/icons/home.svg';
  print('Using: $logo, $homeIcon');
}
